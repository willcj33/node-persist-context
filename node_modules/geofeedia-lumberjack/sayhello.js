const logger = require('nodus-framework').logger;
const errors = require('nodus-framework').errors;

module.exports = name => {

    logger.info('sayhello', {name: name});
    logger.debug('sayhello', {name: name});
    logger.error(errors('TEST_ERROR', {name: name}));

    return `Hello, ${name}!`;
};
